program Adder
const int y = 5, x = 3;
int z;
int br[];
bool t;
char slovo;
enum Broj { JEDAN, DVA }
char nizch[];
{
    void main()
                Broj br;
                int JEDAN, DVA;
                char xy;
        {
       //nizch = new char[3];
       //nizch[0] = 'a';
       //nizch[1] = 'b';
       //nizch[2] = 'c';
       //print(nizch[2 - 1]);
       //print(nizch[1 * 2]);
       //slovo = 'd';
       //print(slovo);
       //read(nizch[0]);
       //print(nizch[0]);
       //br = new int[1];
       //z = 3;
       //z++;
       //print(-z + 1 + 3);
       //z--;
      // print(z);
      // read(br[0]);
      // print(10 - (x + y) + 156 + br[0] * 2);
      // print(-x + ((5 * 1 - 1) * 2 - (3 % 2 + 3 * 2 - 3) - 1 * 0) * 5 / 5);
      // print(t);
      // t = true;
      // print(t);
      // t = false;
      // print(t);
         br = Broj.JEDAN;
         print(br);
         br = Broj.DVA;
         print(br);
    }
}